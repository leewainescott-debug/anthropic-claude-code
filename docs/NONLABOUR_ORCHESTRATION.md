# Non-labour mapping: the ground-up orchestration plan (26/08)

Master plan for placing every AU HW/SW line on the operating model with
zero assumptions. Each phase has a gate; nothing advances past a gate that
has not passed. Lee's rulings D168/D169 govern throughout.

## Phase 0. Data foundation (DONE, proven)
- Cost centre hierarchy solved exactly; parents excluded.
- Leaf truth: SW 51,288,134.47 over 10,138 rows; HW 25,468,998.32 over
  39,772 rows; 76,757,132.79 together.
- Authoritative structure: docs/OP_MODEL_STRUCTURE.md, the only allowed
  portfolio and platform values.
- Gate: totals tie the export's own result rows to the cent. PASSED.

## Phase 1. Evidence extraction (IN FLIGHT)
Six readers, every row read, stems with quoted evidence, OPEN over guess:
1. Ground truth: his 784 rows, the grammar they prove. DONE, tied.
   Verdict: the cost centre decides; products do not override; his
   spellings match the structure doc exactly.
2. SW fuels block (10 centres). DONE, tied to the cent, 472 stems,
   9.79m HIGH / 0.95m OPEN.
3. SW corporate IT centre (25.31m). RUNNING.
4. SW corp functions, marketing, sig centres. RUNNING.
5. HW, all leaf rows. RUNNING.
6. Dashboard control totals, all pages, verbatim. DONE (13 pages, nulls
   where the dashboard hid values behind scrollbars).
- My audit of finished blocks: schema, sums recomputed independently,
  evidence spot-checks against raw (6 of 6 verified), off-chart
  placements (zero). Gate per block: stem sum ties block total to the
  cent AND spot-checks pass. Blocks 1, 2, 6 PASSED.

## Phase 2. Depth pass on the undecided (NEXT)
Every OPEN stem and every line the readers marked shallow gets a second,
deeper read using the row fields Phase 1 under-used:
- The User column (who raised the posting) joined to the labour REVIEW
  names: the requester's team corroborates a home. Corroboration only,
  never sole evidence.
- Purchase order and vendor code joins across tabs (the same PO landing
  in SW and HW ties lines together).
- Posting date patterns (monthly recurring against one-off).
- The 2026 Budget tab's own vendor, detail and IT Service columns as a
  third witness for corporate lines.
- Gate: every remaining OPEN carries a written reason and a dollar
  amount; no OPEN is silently absorbed.

## Phase 3. Adversarial verification
Independent agents that did NOT do the placing:
- Re-derive placements for the 30 biggest stems from scratch; compare.
- Attack a random sample of HIGH placements per block (target: none
  overturned; any overturn sends the whole block back).
- Consistency sweeps: one CO document split across centres must follow
  the centre rule everywhere; ground truth never contradicted; platform
  values never outside the chart.
- Gate: zero unexplained overturns, zero contradictions.

## Phase 4. Controls
- Per old-model area: mapped totals compared against the dashboard's
  budget-view figures at sanity level (different windows, stated on the
  face of the tab; never cent-tied).
- Internal: per-centre ties, grand total 76,757,132.79 exact.
- Gate: every area within explainable distance of its control, with the
  explanation written, or flagged for Lee.

## Phase 5. Integration
The deliverable per D169, built only from gated blocks:
- His five tabs byte-for-byte untouched.
- Added: per-line mapping tab (line ID traced to raw row, portfolio,
  platform, the logic, the evidence, sig-funded flag), totals by
  portfolio and platform, reconciliation (sanity controls), Not 100%
  sure tab with reasons and dollars.
- Gate: mapping tab re-adds to 76,757,132.79 exactly; every line carries
  logic; engine recalc clean; renders eyeballed; his tabs proven
  byte-identical (hash the sheet XML parts against his upload).

## Phase 6. Ship
File to Lee with: the open questions and their dollars in chat, the
area-control comparison, and what remains for the Z/NZ extract.
